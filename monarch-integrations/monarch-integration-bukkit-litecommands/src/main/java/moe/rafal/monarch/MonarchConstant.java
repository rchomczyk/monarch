package moe.rafal.monarch;

import java.util.Locale;

final class MonarchConstant {

    private static final String DEFAULT_LOCALE_TAG = "en-US";
    static final Locale DEFAULT_LOCALE = Locale.forLanguageTag(DEFAULT_LOCALE_TAG);

    private MonarchConstant() {

    }
}
